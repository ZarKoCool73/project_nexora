import { SkillBarDirective } from './skill-bar.directive';

describe('SkillBarDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillBarDirective();
    expect(directive).toBeTruthy();
  });
});
